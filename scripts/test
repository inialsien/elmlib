auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up

auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up


auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up


auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up


auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up


auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up


auto vcan0
   iface vcan0 inet manual
   pre-up /sbin/ip link add dev  type vcan
   up /sbin/ifconfig  up

